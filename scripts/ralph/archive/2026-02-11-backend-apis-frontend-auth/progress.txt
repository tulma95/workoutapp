# Ralph Progress Log

## Codebase Patterns
- Integration tests use real DB via supertest + setup.ts truncation - no mocking needed
- Express 5 params return `string | string[]` - cast with `as string` for single params
- Vite proxy handles /api → localhost:3001, so apiFetch uses relative paths
- Frontend: React 19, react-router-dom v7 (same Route/Outlet API as v6)
- Backend dev server: `npm run dev -w backend` on port 3001
- Frontend dev server: `npm run dev -w frontend` on port 5173
- Test DB runs on port 5433 via docker-compose.test.yml

---

